{
  "type": "module",
  "dependencies": {
    "express": "^4.18.1",
    "requirejs": "^2.3.6",
    "typescript": "^4.7.4"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^16.0.0",
    "gts": "^4.0.0",
    "typescript": "~4.7.0"
  },
  "scripts": {
    "start": "tsc && node ./build/src/app.js",
    "local": "tsc && node ./build/src/index.js",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc -w",
    "fix": "gts fix",
    "prepare": "npm.cmd run compile",
    "pretest": "npm.cmd run compile",
    "posttest": "npm.cmd run lint"
  }
}
